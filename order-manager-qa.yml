server:
  port: 8081
spring:
  data:
    mongodb:
      uri: mongodb://mongodbdatabase:27018/om-data
  cloud:
    stream:
      binders:
        rabbit_binder:
          environment:
            spring:
              rabbitmq:
                host: rabbitmq-oms
                port: 5672
